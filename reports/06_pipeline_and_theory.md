# 06 — 現行パイプラインと理論的背景(2026-07-17 再固定後の正)

🔵再固定(編集可能性=因果証明)と⚫EF除外以降の、理論と実験計画の一本化。
01〜05より新しい決定はすべて本ファイルと PAPER_OUTLINE.md 🔵/⚫節が正。

---

# Part I — 理論的背景

## 1. 研究の目的と貢献の位置づけ

- **前提(目的)**: LinguaLensと同様、言語学的featureに対応するSAE
  activationsを見つけること。**新たな抽出手法は提案しない**。
- **中心主張**: activationsの因果妥当性は**編集によって**評価されるべきで
  あり、これにより評価の幅が広がる — **編集ベース因果評価枠組み**の提案。
- 「評価の幅」の中身: (i) exactという厳密なテキスト接地基準、
  (ii) 介入本数の局在性スペクトル、(iii) net-FRRの特異性、
  (iv) 現象別の到達層(判別木)。
- 貢献文(R5で確定): 「CausalGymの言語学的因果評価を**テキスト編集の
  実行**まで拡張し、SAEBench系の介入評価に**編集という評価器**を加える」。

## 2. 条件付けと介入 — なぜEF系を外したか(⚫)

| | 条件付け(EF/パイプライン/B2) | 介入(steer/clamp) |
|---|---|---|
| 因果式 | P(edit \| Z=z) — zは**入力** | P(Y \| do(Z=z)) — 活性を**書き換える** |
| 検証器 | 学習された編集器(**内生的** — その符号を読むよう最適化済み) | **凍結LM自身(外生的** — 符号体系を見たことがない) |
| 証明できること | zが編集を特定する**情報**を持つ(読み手相対) | LMの計算が**その方向に応答する**(因果) |

- S1(hazard)/S2(特徴トークン)/S3(局所化)は編集器の質の差であって、
  因果構造はすべて条件付けのまま → **EF系は因果証明にならず、論文から
  完全除外**(コード・runsは履歴として残す)。
- EFもLEWIS系パイプラインもB2も「仕様を読んで最小対編集を出す」**同一
  カテゴリ**(違いは精度と条件付けモダリティのみ)— どれも判別の基準に
  使わない。

## 3. バックドア基準と「外生的検証器」問題

- 評価でZ(仕様)は**こちらが設定**する(true/random/empty割付)=入力への
  正真正銘の do(Z)。**バックドア基準は満たす** — Zへの交絡パスは無い。
- 問題は識別ではなく**推定量の帰属先**: 識別されるのは「**この学習済み
  編集器を含むシステム**でZが編集を起こす」であって、「ZがLM/SAE幾何に
  おいてfeatureに対応する」ではない(輸送可能性・構成概念妥当性の問題)。
- 学習された読み手は**単射に近い符号なら何でも**読めるようになる —
  効果の存在が符号(SAE)の性質を弁別しない。
- **SAEのdecodeとの違い**: W_decは再構成のみ学習した**基底変換**で、
  出力は凍結LMの下流計算に食われる(検証はLMが行う)。編集器のdecodeは
  **タスク解読器**(成功するように最適化される)。
- 実例(自前の実測): universal_quantifiers の AUROC=1.000 latent
  (ラベル "proper names and titles")— 読み手ベース評価はこの相関latentを
  「対応」と誤認しうるが、介入は暴いた(0.86×、因果ゼロ)。

## 4. 操作ハンドル ≠ featureに対応するactivations(免許規則)

- **操作ハンドル** = 介入で編集が動く集合。feature方向に加えて**事例の
  内容(WHAT)・整合性維持が混入**する(実測: M0/P-Bでrank17-32が方向と
  内容を分離不能に符号化)。
- したがって「事例レベルkで編集成功 → featureの表現はk本」は**書けない**。
  書けるのは「因果的**操作**にはtop-3では足りない」(操作インターフェース
  幅)まで。
- 「対応する活性はtop-3より多い/別物」を書ける正規ルートは2つ:
  - **形1: 現象レベル(target-free)r掃引** = P-J。現象リストは評価に使わ
    ない別ペア群の統計から作るので**事例内容が構成上混入しない**。実測は
    r単調成長(FRC r=3: 0.17 → r=64: 2.51、常にtrue専有)・64で非飽和 →
    「因果的に有効な現象表現はtop-3より広い」。ただし「対応」の定義を
    検出的(発火統計)→**因果的(介入で動く)**に取り替えたと明示 — この
    取替の必要性こそ本研究の主張の実演(AUROC 0.939のtop-1が因果ゼロ)。
  - **形2: S_minの事例横断安定核**。事例間で共通に生き残るハンドル=現象、
    入れ替わる部分=内容、という操作的分解。安定核をFRC top-3と本数・
    中身で直接比較(compare_smin_frc.py)。

## 5. 3層分解(3ドメイン一致の乖離)

> **検出できる ≠ 指令できる ≠ 介入で書ける**

- 検出: AUROC top-1 mean 0.939(AxBench自身のSAE-A 0.917超え)
- 因果: その完璧なtop-1が介入では動かさない(0.86×)/ FRC3は微弱純粋(67/0)
- 実行: 介入編集が成立する現象は71/98(判別木A類)、WHERE陽性でも介入が
  実行できない現象21(C類=WHAT問題)
- 同型の乖離の先行: Hase et al.(重み編集: localization≠editing)、
  AxBench(steering: 検出+32%でもsteering不変)、我々(離散編集)—
  **3ドメイン一致**として書ける。

## 6. 情報軸(条件付け評価)の扱い — 現状不採用、復活条件つき

- 対応関係(z入力→編集器→出力)の評価は「**情報的忠実性**」として成立
  し得るが、SAEの手柄と主張するには**random辞書対照**(同一レシピで辞書
  だけランダムに差し替えて再学習)が必須。
- 結果A(SAE≫random): 「SAE幾何は編集情報を転移可能な形で担う」(情報軸)。
  結果B(同等): 「読み手ベース評価は辞書を弁別しない → だから介入評価が
  必要」= 主主張のablation的補強。**どちらでも紙面に載る**が、コスト
  (学習~7h+probe)と焦点の判断で現状は不採用。
- 選ばれた活性の正式な呼び方: 「**介入によって因果的に検証された操作
  ハンドル(causal handles)**」。条件付けで読めるという第二の性質は
  主張しない。

## 7. 用語と免許の一覧

| 書いてよい | 書いてはいけない |
|---|---|
| 「同定活性への介入が編集を実行(因果証明)」 | 「EF/routedの成績」を論文に載せる(⚫) |
| 「因果的操作にはtop-3では不十分」 | 「事例k=32がfeatureの表現幅」 |
| 「因果的に有効な現象表現はtop-3より広い(r掃引・定義取替を明示)」 | 検出的な意味での「対応」とすり替える |
| 「介入評価に編集という評価器を加える」 | 「介入ベースSAE評価は初」(SAEBench先行) |
| 「reliabilityを測る」(judge) | 「judge品質/validityを測る」 |

---

# Part II — 現行パイプライン(2026-07-17時点の状態)

## 済み(確定数値)

| 実験 | 守る主張 | 結果 |
|---|---|---|
| **C1'(P-K 2×2)** | **主claim: 介入の編集力は仕様が決める** | 我々の仕様×steer **0.2337**/clamp 0.1743 vs LinguaLens完全版 0.0160・AxBench完全版 0.0701(raw床 0.0601、recon 0.0100) |
| P-N(両論文の指標) | 実装忠実性+主claimの再現 | E_abl +0.631/+0.370 vs 彼ら +0.091/+0.115; 両プロトコルはraw以下〜同等 |
| P-I(学習ゼロreadout) | 因果床(WHERE) | true発火3.3×random、393/31、p=5.6e-81; WHAT不能 |
| P-J(r掃引) | **形1**+選択法対決 | FRC r3: 0.17/0.00(純粋)→r64: 2.51 単調; AUROC r1ゼロ(検出0.939でも) |
| P-B(FRC条件付け対照) | 「3本では足りない」 | 0.0140 vs random 0.0000(事例仕様の~7%) |
| FRR 3judge+McNemar+自己一致 | 特異性+judge信頼性(限界コスト) | steer net-FRR 0.41/自己一致 0.9781 等(**EF行を落とした再集計が必要 — オフライン**) |
| LinguaLens再現(生成+judge) | 再現アンカー | FIC 12.0/8.6/13.6/3.0 vs Table2 8.3/22.9/46.9/6.9 — 弱い行(past_tense・metaphor)がアンカー、強い行は別スタックに転移せず |
| 判別木(paper版) | Limitation判別 | **A 71 / C 21 / B 2 / D 4**(証拠3種: do-介入編集/P-I WHERE/B2フロア) |
| S6学習 | (凍結) | NO-GO — EF除外により論文とは無関係化 |

## 実行中

- **B-1 介入k掃引**(interact-g): steer0.5 × k∈{1,2,4,8,16,32,64}、
  true/empty/random。**局在性スペクトルの操作幅**(免許規則: 表現幅とは
  書かない)。resumeで複数セッション。

## 待機(順番どおり)

1. **AxBench再現 L20**(`run_axbench_repro.sh`): sae/sae_a/ll_set10 の3腕、
   アンカー = 2B L20: SAE 0.151 / SAE-A 0.132(L10: 0.177/0.166)。
   ll_set10 = LinguaLens機構を彼らのベンチで(相互評価の新セル)。
   → prepostでjudge(公式テンプレ逐語、factor選択half/holdout half)
2. **B-2 + S_min×FRC比較**(`run_prune_steer.sh`): steer-exactペア上の
   最小介入集合S_min(max-pairs 120)→ **形2**(安定核×FRC3、
   `runs/tables/smin_vs_frc.md`)
3. AxBench L10、FRR表のEF行なし再集計、reports 01/03/04の全面改訂

## 中止・凍結・任意

- **中止**: ours_ef腕(EF除外+targetなしで事例仕様が定義不能)、P-O EF版
- **凍結**: EF系全部(S3チャンピオン含む — 論文外)、gain-router
- **任意(未採用)**: random辞書対照(情報軸復活の前提、~2セッション)、
  P5単離ablation、BLEU/chrF埋め、P-M(バケット別α)

## 再現アンカーの読み方(共通)

桁一致ではなく**パターンとレンジ**。LinguaLens再現はモデルが違う
(Llama+OpenSAE→gemma+GemmaScope)ので符号・序列・FICスケール;
AxBench再現は**同一スタック**(gemma-2-2b-it+GemmaScope)なので
より強い照合が可能(max_actの出所[Neuronpediaでなくデータセット]と
概念数のみ差)。
